# 🚀 Быстрый старт: Turso (SQLite) - НЕ ЗАБЛОКИРОВАН!

## ✅ Почему Turso:

- ✅ **Есть SQL и таблицы**
- ✅ **Не заблокирован в РФ**
- ✅ **Бесплатный тариф**
- ✅ **Интеграция с Vercel**

## 📋 Что делать СЕЙЧАС:

### Шаг 1: Создать Turso базу

1. **Vercel Dashboard** → **Storage** → **Create Database**
2. В списке найдите **"Turso"** (Serverless SQLite)
3. Нажмите **"Continue"**
4. Название: `course-health-db`
5. Регион: выберите ближайший
6. План: **Free**
7. **Create**

### Шаг 2: Подключить к проекту

1. В созданной Turso базе → **"Connect Project"**
2. Выберите проект: `course-sport`
3. Окружения: ✅ Development, ✅ Preview, ✅ Production
4. Environment Variable: `TURSO_DATABASE_URL` (автоматически)
5. Token: `TURSO_AUTH_TOKEN` (автоматически)
6. **Connect**

### Шаг 3: Выполнить SQL схему

1. В Turso базе откройте **SQL Editor**
2. Скопируйте **весь код** из файла `supabase/schema-turso.sql`
3. Вставьте в SQL Editor
4. Нажмите **"Run"**

Это создаст все таблицы!

### Шаг 4: Установить зависимости

```bash
npm install @libsql/client
```

### Шаг 5: Перезапустить деплой

Vercel Dashboard → Deployments → **Redeploy**

## 📊 Где выполнять SQL:

### В Turso Dashboard:
1. Vercel → Storage → ваша Turso база
2. Откройте базу
3. Найдите **SQL Editor**
4. Выполняйте SQL запросы

### Примеры SQL:

```sql
-- Посмотреть пользователей
SELECT * FROM users;

-- Добавить пользователя
INSERT INTO users (id, name, email) 
VALUES ('uuid-here', 'Иван', 'ivan@example.com');

-- Посмотреть курсы
SELECT * FROM courses WHERE is_published = 1;
```

## ✅ Готово!

После этого:
- ✅ У вас будет SQL база с таблицами
- ✅ Можно выполнять SQL запросы
- ✅ Можно добавлять данные через SQL
- ✅ Не заблокирована в РФ
