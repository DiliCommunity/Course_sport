# 🚀 Руководство по деплою

Этот документ описывает все способы деплоя проекта Course Health.

## Автоматический деплой через Vercel (Рекомендуется)

### Настройка через GitHub

1. **Подключите репозиторий к Vercel:**
   - Перейдите на [vercel.com](https://vercel.com)
   - Нажмите **Add New... → Project**
   - Выберите репозиторий `DiliCommunity/Course_sport`
   - Vercel автоматически определит Next.js

2. **Настройте переменные окружения:**
   - В настройках проекта Vercel перейдите в **Settings → Environment Variables**
   - Добавьте все переменные из `env.local.example`
   - Убедитесь, что переменные добавлены для всех окружений (Production, Preview, Development)

3. **Деплой:**
   - После подключения репозитория, каждый push в `main` автоматически запустит деплой
   - Pull Request создадут Preview деплои для тестирования

### Настройка через Vercel CLI

```bash
# Установите Vercel CLI
npm i -g vercel

# Залогиньтесь
vercel login

# Инициализируйте проект (первый раз)
vercel

# Деплой в production
vercel --prod
```

## GitHub Actions (Альтернативный способ)

Проект включает GitHub Actions workflow для автоматического деплоя.

### Настройка секретов в GitHub

Перейдите в **Settings → Secrets and variables → Actions** и добавьте:

1. **VERCEL_TOKEN** - токен Vercel (получить в Vercel Dashboard → Settings → Tokens)
2. **VERCEL_ORG_ID** - ID организации Vercel
3. **VERCEL_PROJECT_ID** - ID проекта Vercel
4. **NEXT_PUBLIC_SUPABASE_URL** - URL Supabase проекта
5. **NEXT_PUBLIC_SUPABASE_ANON_KEY** - Anon key из Supabase
6. **NEXT_PUBLIC_APP_URL** - URL вашего деплоя
7. **NEXT_PUBLIC_TELEGRAM_BOT_USERNAME** - Username Telegram бота
8. **TELEGRAM_BOT_TOKEN** - Токен Telegram бота

### Как получить Vercel credentials

1. Установите Vercel CLI: `npm i -g vercel`
2. Выполните: `vercel link`
3. Это создаст файл `.vercel/project.json` с нужными ID

## Переменные окружения

### Обязательные переменные

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
TELEGRAM_BOT_TOKEN=your_bot_token
NEXT_PUBLIC_APP_URL=https://your-project.vercel.app
```

### Рекомендуемые переменные

```env
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=YourBotUsername
```

## Проверка деплоя

После успешного деплоя проверьте:

- [ ] Главная страница загружается
- [ ] Все страницы доступны (курсы, категории, эксперты)
- [ ] Анимации работают плавно
- [ ] Telegram Mini App открывается (если настроен)
- [ ] Данные из Supabase загружаются
- [ ] Регистрация/авторизация работает

## Troubleshooting

### Ошибка сборки

- Проверьте логи в Vercel Dashboard
- Убедитесь, что все переменные окружения установлены
- Проверьте, что Node.js версия совместима (18+)

### Ошибка подключения к Supabase

- Проверьте правильность URL и ключей
- Убедитесь, что RLS политики настроены правильно
- Проверьте, что проект Supabase активен

### Telegram Mini App не работает

- Убедитесь, что URL в BotFather совпадает с URL деплоя
- Проверьте HTTPS (обязательно для Telegram)
- Убедитесь, что `NEXT_PUBLIC_APP_URL` установлен правильно

## Дополнительные ресурсы

- [Документация Vercel](https://vercel.com/docs)
- [Next.js Deployment](https://nextjs.org/docs/deployment)
- [GitHub Actions](https://docs.github.com/en/actions)

