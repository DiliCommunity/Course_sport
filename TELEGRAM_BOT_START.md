# 🤖 Настройка команды /start в Telegram боте

## ✅ Что сделано:

1. ✅ Обновлен `/app/api/telegram/webhook/route.ts` - обработка команды `/start`
2. ✅ Добавлено приветственное сообщение с фото кето-диеты
3. ✅ Добавлена кнопка для открытия WebApp
4. ✅ Автоматическое сохранение пользователя в Supabase

## 📋 Настройка Webhook в BotFather:

### Шаг 1: Получить токен бота

1. Откройте **@BotFather** в Telegram
2. Отправьте команду `/mybots`
3. Выберите вашего бота: **@Course_Sport_bot**
4. Выберите **"API Token"**
5. Скопируйте токен (или используйте существующий)

### Шаг 2: Настроить Webhook

⚠️ **Важно:** `/setwebhook` - это НЕ команда BotFather! Это метод Telegram Bot API.

**Способ 1: Через скрипт (рекомендуется)**

1. Запустите скрипт из корня проекта:
   ```bash
   node setup-webhook.js YOUR_BOT_TOKEN https://course-sport.vercel.app/api/telegram/webhook
   ```

**Способ 2: Через curl (в терминале)**

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://course-sport.vercel.app/api/telegram/webhook"
  }'
```

**Способ 3: Через браузер (самый простой)**

Откройте в браузере (замените `YOUR_BOT_TOKEN`):
```
https://api.telegram.org/botYOUR_BOT_TOKEN/setWebhook?url=https://course-sport.vercel.app/api/telegram/webhook
```

Должен вернуться: `{"ok":true,"result":true,"description":"Webhook was set"}`

📖 **Подробная инструкция:** См. файл `TELEGRAM_WEBHOOK_SETUP.md`

### Шаг 3: Добавить переменные в Vercel

В **Vercel Dashboard** → **Settings** → **Environment Variables**:

| Name | Value | Environment |
|------|-------|-------------|
| `TELEGRAM_BOT_TOKEN` | `ваш_токен_из_BotFather` | ✅ Production, ✅ Preview, ✅ Development |
| `TELEGRAM_WEBHOOK_SECRET` | `случайная_строка_для_безопасности` (опционально) | ✅ Production, ✅ Preview, ✅ Development |
| `NEXT_PUBLIC_SITE_URL` | `https://course-sport.vercel.app` | ✅ Production, ✅ Preview, ✅ Development |

**Важно:** 
- `TELEGRAM_BOT_TOKEN` - обязателен для работы бота
- `TELEGRAM_WEBHOOK_SECRET` - опционален, но рекомендуется для безопасности
- `NEXT_PUBLIC_SITE_URL` - должен указывать на ваш домен Vercel

### Шаг 4: Проверить Webhook

```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
```

Должен вернуть информацию о настроенном webhook.

---

## 🎉 Что происходит при /start:

1. **Пользователь отправляет `/start`** в боте **@Course_Sport_bot**

2. **Бот отправляет приветственное сообщение:**
   - 📸 **Фото кето-диеты** из `/public/img/keto_course.png`
   - 💬 **Текст с приветствием:**
     ```
     🎉 Привет, [Имя]!
     
     🔥 Добро пожаловать в Course Health — твой путь к идеальной форме!
     
     🥑 Что тебя ждет:
     • Кето-диета: наука жиросжигания
     • Интервальное голодание: режим дня для энергии
     • Вкусные рецепты и планы питания
     • Отслеживание прогресса
     
     💪 15% контента бесплатно!
     Полный доступ за 1500₽
     
     🚀 Нажми кнопку ниже, чтобы начать обучение прямо сейчас!
     ```
   - 🔘 **Кнопка "🚀 Открыть приложение"** - открывает WebApp в Telegram
   - 🔘 **Кнопка "📚 Посмотреть курсы"** - ссылка на страницу курсов
   - 🔘 **Кнопка "💚 О нас"** - ссылка на страницу "О нас"

3. **Автоматически сохраняется в Supabase:**
   - `telegram_id` - ID пользователя Telegram
   - `name` - Имя пользователя
   - `telegram_username` - Username (если есть)
   - `telegram_verified: true`

---

## 🎨 Кастомизация сообщения:

Вы можете изменить текст приветствия в файле:
`app/api/telegram/webhook/route.ts`

Функция `handleStartCommand()` содержит:
- `welcomeText` - текст приветствия (поддерживает HTML)
- `photoUrl` - URL изображения
- `replyMarkup` - кнопки под сообщением

---

## 🔧 Дополнительные команды:

Можно добавить обработку других команд:

```typescript
// В функции POST, после обработки /start:

if (text === '/help') {
  await sendTelegramMessage(chatId, 'Помощь по боту...')
  return NextResponse.json({ success: true })
}

if (text === '/courses') {
  // Отправить список курсов
  return NextResponse.json({ success: true })
}
```

---

## 📸 Изображение:

Используется изображение: `/public/img/keto_course.png`

Если хотите использовать другое изображение:
1. Загрузите изображение в `/public/img/`
2. Измените `photoUrl` в функции `handleStartCommand()`

---

## ✅ Проверка работы:

1. Откройте бота **@Course_Sport_bot** в Telegram
2. Отправьте команду `/start`
3. Должно прийти сообщение с фото и кнопками
4. Нажмите "🚀 Открыть приложение" - должно открыться WebApp

---

## 🐛 Если не работает:

1. **Проверьте токен бота:**
   - Убедитесь, что `TELEGRAM_BOT_TOKEN` правильно добавлен в Vercel
   - Проверьте, что токен активен в BotFather

2. **Проверьте webhook:**
   ```bash
   curl "https://api.telegram.org/bot<TOKEN>/getWebhookInfo"
   ```
   - Должен быть установлен правильный URL
   - Не должно быть ошибок

3. **Проверьте логи Vercel:**
   - Deployments → View Function Logs
   - Ищите ошибки в `/api/telegram/webhook`

4. **Проверьте переменные окружения:**
   - `TELEGRAM_BOT_TOKEN` должен быть установлен
   - `NEXT_PUBLIC_SITE_URL` должен указывать на ваш домен

---

## 🎯 Готово!

После настройки webhook, при команде `/start` пользователь получит красивое приветственное сообщение с фото и кнопками для открытия приложения!
