# 📖 Как посмотреть сайт и где находятся HTML файлы

## 🔍 Где находятся HTML файлы?

В Next.js 14 с App Router **HTML файлы генерируются автоматически** при сборке проекта. Они находятся в папке `.next` после выполнения команды `npm run build`.

### Структура после сборки:

```
.next/
├── static/          # Статические файлы (CSS, JS, изображения)
├── server/          # Серверные компоненты
└── ...              # Другие файлы сборки
```

**⚠️ ВАЖНО:** Не редактируйте файлы в `.next` вручную! Они генерируются автоматически.

## 🚀 Как посмотреть сайт

### 1. Локальная разработка

```bash
# Запустите dev сервер
npm run dev

# Откройте в браузере
http://localhost:3000
```

### 2. Production сборка (локально)

```bash
# Соберите проект
npm run build

# Запустите production сервер
npm start

# Откройте в браузере
http://localhost:3000
```

### 3. На Vercel (после деплоя)

1. Перейдите в Vercel Dashboard
2. Выберите проект
3. Скопируйте URL деплоя (например: `https://course-sport.vercel.app`)
4. Откройте в браузере

## 📄 Исходные файлы страниц

HTML генерируется из React компонентов в папке `app/`:

```
app/
├── page.tsx              → Главная страница (/)
├── about/page.tsx        → Страница "О нас" (/about)
├── courses/
│   ├── page.tsx          → Список курсов (/courses)
│   └── [id]/page.tsx     → Страница курса (/courses/1)
├── login/page.tsx        → Страница входа (/login)
└── register/page.tsx     → Страница регистрации (/register)
```

## 🔧 Как посмотреть сгенерированный HTML

### Вариант 1: Через DevTools браузера

1. Откройте сайт в браузере
2. Нажмите `F12` или `Ctrl+Shift+I` (Windows) / `Cmd+Option+I` (Mac)
3. Перейдите на вкладку **Elements** (Chrome) или **Inspector** (Firefox)
4. Там вы увидите весь HTML код страницы

### Вариант 2: Посмотреть исходный код

1. Откройте сайт в браузере
2. Нажмите `Ctrl+U` (Windows) / `Cmd+Option+U` (Mac)
3. Или правой кнопкой мыши → "Просмотр кода страницы"

### Вариант 3: После сборки

```bash
# Соберите проект
npm run build

# HTML файлы будут в .next/server/app/
# Но они в специальном формате Next.js, не обычный HTML
```

## 🎨 Структура компонентов

Все компоненты находятся в папке `components/`:

```
components/
├── layout/           # Header, Footer
├── sections/         # Hero, FeaturedCourses, etc.
├── ui/              # Button, CourseCard, etc.
└── providers/       # TelegramProvider, AuthProvider
```

## 📱 Проверка работы сайта

### Локально:
1. Убедитесь, что все переменные окружения в `.env.local` заполнены
2. Запустите `npm run dev`
3. Откройте `http://localhost:3000`

### На Vercel:
1. Убедитесь, что все переменные окружения добавлены в Vercel Dashboard
2. Дождитесь успешного деплоя
3. Откройте URL проекта

## 🐛 Если сайт не отображается

1. **Проверьте консоль браузера** (F12 → Console) на наличие ошибок
2. **Проверьте Network вкладку** (F12 → Network) - загружаются ли файлы
3. **Проверьте переменные окружения** - все ли заполнено
4. **Проверьте логи Vercel** - есть ли ошибки при деплое

## 💡 Полезные команды

```bash
# Очистить кэш и пересобрать
rm -rf .next
npm run build

# Проверить типы TypeScript
npm run type-check

# Проверить линтер
npm run lint
```

---

**Помни:** В Next.js HTML генерируется динамически из React компонентов. Не нужно искать статические HTML файлы - их нет! Всё рендерится на лету или при сборке.

