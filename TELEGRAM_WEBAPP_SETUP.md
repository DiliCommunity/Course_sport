# 🤖 Настройка Telegram WebApp для автоматической регистрации

## ✅ Что сделано:

1. ✅ Создан `public/js/telegram-webapp.js` - автоматическая авторизация через Telegram WebApp
2. ✅ Обновлен `AuthProvider` - автоматическая авторизация при открытии WebApp
3. ✅ Обновлен API `/api/auth/telegram` - поддержка `phone_number` из Telegram
4. ✅ Добавлена синхронизация между WebApp и браузером через localStorage и cookies

## 📋 Настройка Telegram бота:

### Шаг 1: Настроить WebApp в BotFather

1. Откройте **@BotFather** в Telegram
2. Отправьте команду `/mybots`
3. Выберите вашего бота: **@Course_Sport_bot**
4. Выберите **"Bot Settings"** → **"Menu Button"**
5. Настройте кнопку меню:
   - **Text**: "Открыть приложение" или "Course Health"
   - **URL**: `https://your-domain.com` (ваш домен на Vercel)

Или используйте команду:
```
/setmenubutton
@Course_Sport_bot
Открыть приложение
https://your-domain.com
```

### Шаг 2: Настроить WebApp URL

В BotFather:
```
/setdomain
@Course_Sport_bot
your-domain.com
```

## 🔄 Как это работает:

### 1. Открытие через Telegram бота:

1. Пользователь открывает бота **@Course_Sport_bot**
2. Нажимает кнопку меню или переходит по ссылке
3. Открывается WebApp в Telegram
4. **Автоматически** выполняется:
   - Инициализация Telegram WebApp
   - Получение данных пользователя из `tg.initDataUnsafe.user`
   - Автоматическая регистрация/авторизация через `/api/auth/telegram`
   - Создание сессии в Supabase
   - Сохранение в localStorage для синхронизации

### 2. Синхронизация с браузером:

- **Cookies**: Сессия Supabase сохраняется в cookies
- **localStorage**: Сохраняются `telegram_auth`, `telegram_user_id`, `user_id`
- При открытии сайта в браузере проверяется сессия и localStorage
- Если сессия валидна - пользователь автоматически авторизован

### 3. Данные из Telegram:

- `id` - ID пользователя Telegram
- `first_name` - Имя
- `last_name` - Фамилия (если есть)
- `username` - Username (если есть)
- `photo_url` - Фото профиля (если есть)
- `phone_number` - Номер телефона (если пользователь разрешил)

## 📱 Использование:

### В Telegram WebApp:

При открытии через бота автоматически:
1. Регистрируется/авторизуется пользователь
2. Создается профиль в Supabase
3. Сохраняется сессия
4. Перенаправляется на профиль

### В браузере:

1. Пользователь открывает сайт в браузере
2. Проверяется сессия из cookies
3. Если сессия есть - авторизован
4. Если нет - показывается форма входа

### Синхронизация:

Если пользователь сначала открыл WebApp, а потом браузер:
- Сессия из cookies автоматически подхватывается
- localStorage помогает определить, что пользователь авторизован через Telegram

## 🔧 API Endpoints:

### POST `/api/auth/telegram`

**Тело запроса:**
```json
{
  "id": 123456789,
  "first_name": "Иван",
  "last_name": "Иванов",
  "username": "ivan",
  "photo_url": "https://...",
  "phone_number": "+79001234567"
}
```

**Ответы:**

1. **Новый пользователь (без телефона):**
```json
{
  "success": true,
  "user": { ... },
  "session": { ... }
}
```

2. **Новый пользователь (с телефоном):**
```json
{
  "success": true,
  "requires_otp": true,
  "message": "OTP code sent to your phone"
}
```

3. **Существующий пользователь:**
```json
{
  "success": true,
  "user_id": "uuid",
  "telegram_id": "123456789",
  "session": { ... }
}
```

## ✅ Готово!

После настройки бота:
- ✅ Пользователи могут открывать приложение через Telegram бота
- ✅ Автоматическая регистрация/авторизация
- ✅ Синхронизация с браузером
- ✅ Работает на всех страницах (index.html, profile.html, login.html, register.html)

## 🔗 Ссылка на бота:

**t.me/Course_Sport_bot**

После настройки WebApp в BotFather, пользователи смогут открывать приложение прямо из Telegram!
