# 🤖 Настройка Telegram бота для автоматической регистрации

## ✅ Что сделано:

1. ✅ Создан `public/js/telegram-webapp.js` - автоматическая авторизация
2. ✅ Обновлен API `/api/auth/telegram` - поддержка `phone_number`
3. ✅ Добавлена синхронизация через cookies и localStorage
4. ✅ Скрипт добавлен во все HTML страницы

## 📋 Настройка бота в BotFather:

### Шаг 1: Настроить WebApp URL

1. Откройте **@BotFather** в Telegram
2. Отправьте команду:
```
/setmenubutton
@Course_Sport_bot
Открыть приложение
https://your-domain.vercel.app
```

Или через меню:
- `/mybots` → выберите `@Course_Sport_bot`
- **"Bot Settings"** → **"Menu Button"**
- **Text**: "Открыть приложение"
- **URL**: `https://your-domain.vercel.app`

### Шаг 2: Настроить домен (опционально)

```
/setdomain
@Course_Sport_bot
your-domain.vercel.app
```

## 🔄 Как это работает:

### 1. Пользователь открывает бота:

1. Пользователь открывает **@Course_Sport_bot** в Telegram
2. Нажимает кнопку меню или переходит по ссылке
3. Открывается WebApp в Telegram
4. **Автоматически** выполняется:
   - Загрузка скрипта `telegram-web-app.js`
   - Инициализация WebApp (`tg.ready()`, `tg.expand()`)
   - Получение данных пользователя из `tg.initDataUnsafe.user`
   - Автоматический запрос на `/api/auth/telegram`
   - Создание/обновление пользователя в Supabase
   - Создание сессии (если возможно)
   - Сохранение в localStorage
   - Перенаправление на профиль

### 2. Данные из Telegram WebApp:

```javascript
{
  id: 123456789,
  first_name: "Иван",
  last_name: "Иванов",
  username: "ivan",
  photo_url: "https://...",
  phone_number: "+79001234567" // если пользователь разрешил
}
```

### 3. Автоматическая регистрация:

**Новый пользователь:**
- Если есть `phone_number` → отправляется OTP на телефон
- Если нет телефона → создается пользователь с временным email
- Автоматически создается профиль в `public.users`
- Автоматически создается баланс в `user_balance`

**Существующий пользователь:**
- Обновляются данные (имя, username, фото)
- Проверяется сессия
- Если сессии нет и есть телефон → можно запросить OTP

### 4. Синхронизация с браузером:

**Cookies:**
- Сессия Supabase сохраняется в HTTP-only cookies
- Работает между WebApp и браузером

**localStorage:**
- `telegram_auth: 'true'` - флаг авторизации через Telegram
- `telegram_user_id: '123456789'` - ID пользователя Telegram
- `user_id: 'uuid'` - ID пользователя в системе

**При открытии в браузере:**
1. Проверяется сессия из cookies
2. Если сессии нет, проверяется localStorage
3. Если есть `telegram_auth`, выполняется синхронизация

## 📱 Использование:

### В Telegram WebApp:

При открытии через бота:
- ✅ Автоматическая регистрация/авторизация
- ✅ Создание профиля
- ✅ Перенаправление на профиль
- ✅ Уведомление пользователю

### В браузере:

1. Пользователь открывает сайт
2. Проверяется сессия
3. Если авторизован - показывается профиль
4. Если нет - форма входа

### Синхронизация:

Если пользователь сначала открыл WebApp, а потом браузер:
- Сессия из cookies автоматически подхватывается
- localStorage помогает определить авторизацию
- Пользователь остается авторизованным

## 🔧 API Endpoint:

### POST `/api/auth/telegram`

**Тело запроса:**
```json
{
  "id": 123456789,
  "first_name": "Иван",
  "last_name": "Иванов",
  "username": "ivan",
  "photo_url": "https://...",
  "phone_number": "+79001234567"
}
```

**Ответы:**

1. **Новый пользователь (без телефона):**
```json
{
  "success": true,
  "user": { "id": "uuid", ... },
  "session": { ... }
}
```

2. **Новый пользователь (с телефоном):**
```json
{
  "success": true,
  "requires_otp": true,
  "message": "OTP code sent to your phone"
}
```

3. **Существующий пользователь:**
```json
{
  "success": true,
  "user_id": "uuid",
  "telegram_id": "123456789",
  "session": { ... }
}
```

## ✅ Готово!

После настройки бота:
- ✅ Пользователи открывают приложение через Telegram
- ✅ Автоматическая регистрация/авторизация
- ✅ Синхронизация с браузером
- ✅ Работает на всех страницах

## 🔗 Ссылка на бота:

**t.me/Course_Sport_bot**

После настройки WebApp в BotFather пользователи смогут открывать приложение прямо из Telegram!
