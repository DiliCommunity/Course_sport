# 🚀 Настройка Turso (SQLite) - НЕ ЗАБЛОКИРОВАН В РФ!

## ✅ Почему Turso:

- ✅ **Есть SQL и таблицы**
- ✅ **Не заблокирован в РФ**
- ✅ **Бесплатный тариф**
- ✅ **Интеграция с Vercel**
- ✅ **Быстрый и надежный**

## 📋 Пошаговая инструкция:

### Шаг 1: Создать Turso базу

1. **Vercel Dashboard** → **Storage** → **Create Database**
2. В списке найдите **"Turso"** (Serverless SQLite)
3. Нажмите **"Continue"**
4. Заполните:
   - **Name**: `course-health-db`
   - **Region**: выберите ближайший
   - **Plan**: Free
5. Нажмите **"Create"**

### Шаг 2: Подключить к проекту

1. В созданной Turso базе → **"Connect Project"**
2. Выберите проект: `course-sport`
3. Окружения: ✅ Development, ✅ Preview, ✅ Production
4. Environment Variable: `TURSO_DATABASE_URL` (автоматически)
5. Token: `TURSO_AUTH_TOKEN` (автоматически)
6. Нажмите **"Connect"**

### Шаг 3: Установить зависимости

```bash
npm install @libsql/client
```

### Шаг 4: Выполнить SQL схему

1. В Turso базе откройте **SQL Editor**
2. Скопируйте код из `supabase/schema.sql`
3. **ВАЖНО:** Замените `uuid_generate_v4()` на `(lower(hex(randomblob(4))) || '-' || lower(hex(randomblob(2))) || '-4' || substr(lower(hex(randomblob(2))),2) || '-' || substr('89ab',abs(random()) % 4 + 1, 1) || substr(lower(hex(randomblob(2))),2) || '-' || lower(hex(randomblob(6))))`
4. Или используйте простые UUID строки
5. Выполните SQL

### Шаг 5: Обновить код

Я создам `lib/turso/db.ts` для работы с Turso.

## 📝 SQL в Turso:

Turso использует SQLite синтаксис, который очень похож на PostgreSQL:

```sql
-- Создать таблицу
CREATE TABLE users (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  email TEXT UNIQUE
);

-- Добавить данные
INSERT INTO users (id, name, email) 
VALUES ('uuid-here', 'Иван', 'ivan@example.com');

-- Посмотреть данные
SELECT * FROM users;
```

## ✅ Готово!

После настройки у вас будет:
- ✅ SQL база данных
- ✅ Таблицы
- ✅ Возможность выполнять SQL запросы
- ✅ Не заблокирована в РФ

Хотите, чтобы я переделал код под Turso?
