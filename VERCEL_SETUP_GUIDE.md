# 📋 Пошаговая инструкция настройки Vercel KV и Blob Storage

## ⚠️ ВАЖНО: Вы создали Neon (PostgreSQL), а нужен KV!

На скриншотах видно, что вы создаете **Neon Database** (PostgreSQL), но для проекта нужен **Vercel KV** (Redis key-value хранилище).

## ✅ Правильная последовательность действий:

### Шаг 1: Создать Vercel KV

1. В Vercel Dashboard перейдите в **Storage**
2. Нажмите **"Create Database"**
3. Выберите **"KV"** (НЕ Neon!)
4. Назовите базу: `course-health-kv`
5. Выберите регион (например, Washington, D.C.)
6. Нажмите **"Create"**

### Шаг 2: Подключить проект к KV

1. В созданном KV нажмите **"Connect Project"**
2. В поиске введите `course-sport` и выберите проект
3. Выберите окружения:
   - ✅ Development
   - ✅ Preview  
   - ✅ Production
4. В **Advanced Options**:
   - Environment Variable: `KV_REST_API_URL` (по умолчанию)
   - Token Label: `course-sport-kv-token`
5. Нажмите **"Connect"**

### Шаг 3: Получить токены

После подключения:

1. Перейдите в KV → **Settings** → **Tokens**
2. Скопируйте:
   - `KV_REST_API_URL` (URL для подключения)
   - `KV_REST_API_TOKEN` (токен доступа)

### Шаг 4: Добавить переменные в проект

1. В Vercel Dashboard перейдите в ваш проект `course-sport`
2. Откройте **Settings** → **Environment Variables**
3. Добавьте переменные:

```
KV_REST_API_URL = (скопированное значение из KV Settings)
KV_REST_API_TOKEN = (скопированное значение из KV Settings)
```

4. Выберите окружения: **Development**, **Preview**, **Production**
5. Нажмите **"Save"**

### Шаг 5: Создать Blob Storage (для аватаров)

1. В Vercel Dashboard → **Storage** → **Create Database**
2. Выберите **"Blob"** (не KV, не Neon!)
3. Назовите: `course-health-blob`
4. Подключите проект `course-sport` аналогично KV
5. Получите `BLOB_READ_WRITE_TOKEN` из Settings
6. Добавьте в Environment Variables проекта:
   ```
   BLOB_READ_WRITE_TOKEN = (токен из Blob Settings)
   ```

### Шаг 6: Перезапустить деплой

После добавления всех переменных:

1. Перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите **"Redeploy"** (или сделайте новый коммит)

## 🔍 Проверка

После деплоя проверьте:

1. Регистрация пользователя работает: `/api/auth/register`
2. Вход работает: `/api/auth/login`
3. Профиль загружается: `/api/profile/data`

## ❓ Что делать с Neon?

Если вы уже создали Neon Database:
- Можно оставить его (не мешает)
- Или удалить через Storage → ваша Neon база → Settings → Delete

**Главное:** Используйте **KV** для данных проекта, а не Neon!

## 📝 Итоговые переменные окружения

В проекте должны быть:

```
KV_REST_API_URL=...
KV_REST_API_TOKEN=...
BLOB_READ_WRITE_TOKEN=...
```

Эти переменные автоматически доступны в коде через `@vercel/kv` и `@vercel/blob`.
