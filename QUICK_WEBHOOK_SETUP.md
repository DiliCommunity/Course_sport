# ⚡ Быстрая настройка Webhook (1 минута)

## 🎯 Самый простой способ:

### 1. Получите токен бота:
- Откройте **@BotFather** в Telegram
- Отправьте `/mybots`
- Выберите **@Course_Sport_bot**
- Выберите **"API Token"**
- Скопируйте токен (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Настройте webhook через браузер:

Откройте эту ссылку в браузере (замените `YOUR_BOT_TOKEN` на ваш токен):

```
https://api.telegram.org/botYOUR_BOT_TOKEN/setWebhook?url=https://course-sport.vercel.app/api/telegram/webhook
```

**Пример:**
```
https://api.telegram.org/bot123456789:ABCdefGHIjklMNOpqrsTUVwxyz/setWebhook?url=https://course-sport.vercel.app/api/telegram/webhook
```

### 3. Проверьте результат:

Должен вернуться JSON:
```json
{"ok":true,"result":true,"description":"Webhook was set"}
```

Если видите `{"ok":true}` - всё готово! ✅

### 4. Проверьте webhook:

Откройте в браузере (замените `YOUR_BOT_TOKEN`):
```
https://api.telegram.org/botYOUR_BOT_TOKEN/getWebhookInfo
```

Должен показать ваш URL webhook.

### 5. Добавьте переменные в Vercel:

1. Откройте **Vercel Dashboard** → Ваш проект
2. **Settings** → **Environment Variables**
3. Добавьте:

| Name | Value |
|------|-------|
| `TELEGRAM_BOT_TOKEN` | Ваш токен из BotFather |
| `NEXT_PUBLIC_SITE_URL` | `https://course-sport.vercel.app` |

### 6. Сделайте Redeploy на Vercel

### 7. Протестируйте:

Откройте бота **@Course_Sport_bot** и отправьте `/start` - должно прийти приветственное сообщение! 🎉

---

## 🐛 Если не работает:

1. **Проверьте токен** - убедитесь, что скопировали полностью
2. **Проверьте URL** - должен быть `https://` (не `http://`)
3. **Проверьте переменные** - должны быть добавлены в Vercel
4. **Сделайте redeploy** - после добавления переменных

---

## 📖 Подробная инструкция:

См. файл `TELEGRAM_WEBHOOK_SETUP.md` для всех способов настройки.
