# 📝 Как работать с SQL в Neon PostgreSQL

## ✅ У вас уже есть Neon Database!

База данных **`db-course-health`** (Neon PostgreSQL) уже создана. Теперь нужно:

## 🔍 Где выполнять SQL код:

### Способ 1: В Vercel Dashboard (РЕКОМЕНДУЕТСЯ)

1. Откройте **Vercel Dashboard**
2. Перейдите в **Storage**
3. Найдите базу **`db-course-health`** (Neon)
4. Нажмите на неё
5. В боковом меню найдите **"SQL Editor"** или **"Query"**
6. Вставьте SQL код
7. Нажмите **"Run"** или **"Execute"**

### Способ 2: В Neon Dashboard

1. Перейдите на **neon.tech**
2. Войдите (можно через Vercel аккаунт)
3. Выберите ваш проект
4. Выберите базу данных
5. Откройте **"SQL Editor"**
6. Выполняйте SQL запросы

### Способ 3: Через переменную окружения

После подключения Neon к проекту, в Vercel появятся переменные:
- `STORAGE_URL` - строка подключения к базе

Можно использовать любой PostgreSQL клиент с этой строкой.

## 📋 Первый шаг: Создать таблицы

### 1. Подключите Neon к проекту:

1. В Vercel → Storage → ваша Neon база
2. **"Connect Project"**
3. Выберите проект `course-sport`
4. Окружения: Development, Preview, Production
5. Environment Variable: `STORAGE_URL`
6. **"Connect"**

### 2. Выполните SQL схему:

1. Откройте **SQL Editor** в Neon
2. Скопируйте **весь код** из файла `supabase/schema.sql`
3. Вставьте в SQL Editor
4. Нажмите **"Run"**

Это создаст все таблицы:
- ✅ `users` - пользователи
- ✅ `courses` - курсы  
- ✅ `enrollments` - записи на курсы
- ✅ `user_balance` - баланс
- ✅ `referrals` - рефералы
- ✅ `transactions` - транзакции
- ✅ `sessions` - сессии
- И другие...

## 💡 Примеры SQL запросов:

### Посмотреть всех пользователей:
```sql
SELECT * FROM users;
```

### Добавить пользователя:
```sql
INSERT INTO users (name, email) 
VALUES ('Иван Иванов', 'ivan@example.com');
```

### Посмотреть курсы:
```sql
SELECT * FROM courses WHERE is_published = true;
```

### Добавить курс:
```sql
INSERT INTO courses (title, description, short_description, price, image_url, category_id, instructor_id, duration_minutes, is_published)
VALUES (
  'Название курса',
  'Полное описание',
  'Краткое описание',
  1500,
  '/img/course.png',
  (SELECT id FROM categories LIMIT 1),
  (SELECT id FROM instructors LIMIT 1),
  1200,
  true
);
```

### Посмотреть баланс пользователя:
```sql
SELECT * FROM user_balance WHERE user_id = 'user-id-here';
```

### Посмотреть транзакции:
```sql
SELECT * FROM transactions ORDER BY created_at DESC LIMIT 10;
```

## 🔄 Что дальше?

После создания таблиц нужно:
1. Установить драйвер: `npm install @neondatabase/serverless`
2. Обновить код для использования Neon вместо Redis
3. Перезапустить деплой

Хотите, чтобы я переделал код под Neon PostgreSQL?
