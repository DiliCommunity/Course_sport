# 🚀 Быстрая настройка Upstash Redis

## Что делать прямо сейчас:

### 1. Создать Upstash Redis в Vercel

1. **Vercel Dashboard** → **Storage** → **Create Database**
2. В списке найдите **"Upstash"** (Serverless DB Redis)
3. Нажмите **"Continue"**
4. Название: `course-health-redis`
5. Регион: выберите ближайший
6. План: **Free**
7. **Create**

### 2. Подключить проект

1. В созданной Upstash базе → **"Connect Project"**
2. Выберите проект: `course-sport`
3. Окружения: ✅ Development, ✅ Preview, ✅ Production
4. Нажмите **"Connect"**

### 3. Переменные появятся автоматически!

После подключения Vercel автоматически добавит:
- `UPSTASH_REDIS_REST_URL`
- `UPSTASH_REDIS_REST_TOKEN`

### 4. Установить зависимости

```bash
npm install @upstash/redis
```

### 5. Перезапустить деплой

В Vercel Dashboard → **Deployments** → **Redeploy**

## ✅ Готово!

После этого профиль будет работать правильно для каждого пользователя отдельно.

## 🔍 Проверка профиля

Проблема была в том, что:
- Сессии не работали (KV не был настроен)
- Все пользователи видели одинаковые данные

Теперь:
- ✅ Каждый пользователь видит свой профиль
- ✅ Данные изолированы по user_id
- ✅ Сессии работают через Redis
