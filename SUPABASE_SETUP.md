# 🚀 Настройка Supabase для проекта

## ✅ Что нужно сделать:

### Шаг 1: Установить зависимости

```bash
npm install @supabase/supabase-js @supabase/ssr
npm uninstall @libsql/client @vercel/blob
```

### Шаг 2: Настроить переменные окружения

Добавьте в `.env.local` и в Vercel Dashboard:

```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
```

### Шаг 3: Выполнить SQL схему

1. Откройте **Supabase Dashboard** → **SQL Editor**
2. Скопируйте весь код из `supabase/schema.sql`
3. Вставьте и выполните

Это создаст:
- ✅ Таблицы для пользователей, курсов, баланса, рефералов
- ✅ RLS политики для безопасности
- ✅ Триггеры для автоматического создания профиля и баланса
- ✅ Индексы для быстрого поиска

### Шаг 4: Настроить авторизацию в Supabase

1. **Supabase Dashboard** → **Authentication** → **Providers**
2. Включите:
   - ✅ **Email** (уже включен)
   - ✅ **Phone** (SMS) - включите и настройте провайдера (Twilio, MessageBird и т.д.)
   - ✅ **Telegram** - можно использовать через OAuth или кастомную логику

### Шаг 5: Настроить SMS провайдера (для авторизации по телефону)

1. **Authentication** → **Phone Auth**
2. Выберите провайдера (Twilio, MessageBird, Vonage)
3. Введите API ключи
4. Настройте шаблон SMS сообщения

## 📱 Методы авторизации:

### 1. По Email
- Пользователь вводит email и пароль
- Supabase создает пользователя в `auth.users`
- Триггер автоматически создает профиль в `public.users`

### 2. По телефону
- Пользователь вводит номер телефона
- Supabase отправляет SMS с кодом
- После подтверждения создается пользователь
- Триггер автоматически создает профиль

### 3. Через Telegram
- Пользователь авторизуется через Telegram
- Получаем `telegram_id` и `phone` из Telegram
- Создаем пользователя в Supabase с телефоном
- Сохраняем `telegram_id` в профиле

## 🔐 Безопасность:

- ✅ RLS политики защищают данные
- ✅ Пользователи видят только свои данные
- ✅ Автоматическая проверка через `auth.uid()`

## 📊 Структура данных:

- `auth.users` - пользователи Supabase (email, phone, пароли)
- `public.users` - расширенный профиль (имя, аватар, telegram_id)
- `user_balance` - баланс пользователя
- `referrals` - реферальная система
- `transactions` - история транзакций
