# ⚙️ Настройка автоматического деплоя

Этот документ поможет настроить автоматический деплой для проекта Course Health.

## 🎯 Вариант 1: Автоматический деплой через Vercel (Самый простой)

Vercel автоматически деплоит проект при каждом push в `main` ветку.

### Шаги:

1. **Подключите репозиторий к Vercel:**
   - Перейдите на [vercel.com](https://vercel.com)
   - Войдите через GitHub
   - Нажмите **Add New... → Project**
   - Выберите репозиторий `DiliCommunity/Course_sport`

2. **Настройте переменные окружения:**
   - В настройках проекта: **Settings → Environment Variables**
   - Добавьте все переменные из `env.local.example`
   - **Важно:** Добавьте переменные для всех окружений (Production, Preview, Development)

3. **Деплой:**
   - После первого подключения, Vercel автоматически задеплоит проект
   - Каждый push в `main` будет автоматически деплоиться
   - Pull Request создадут Preview деплои

**Готово!** Теперь каждый push будет автоматически деплоиться. 🎉

## 🔧 Вариант 2: GitHub Actions (Для продвинутых)

Если вы хотите использовать GitHub Actions для деплоя:

### Шаги:

1. **Получите Vercel credentials:**
   ```bash
   npm i -g vercel
   vercel login
   vercel link
   ```
   Это создаст файл `.vercel/project.json` с нужными ID.

2. **Добавьте секреты в GitHub:**
   - Перейдите в репозиторий: **Settings → Secrets and variables → Actions**
   - Нажмите **New repository secret**
   - Добавьте следующие секреты:

   | Секрет | Описание | Где взять |
   |--------|----------|-----------|
   | `VERCEL_TOKEN` | Токен Vercel | Vercel Dashboard → Settings → Tokens → Create Token |
   | `VERCEL_ORG_ID` | ID организации | Файл `.vercel/project.json` после `vercel link` |
   | `VERCEL_PROJECT_ID` | ID проекта | Файл `.vercel/project.json` после `vercel link` |
   | `NEXT_PUBLIC_SUPABASE_URL` | URL Supabase | Supabase Dashboard → Settings → API |
   | `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Anon key | Supabase Dashboard → Settings → API |
   | `NEXT_PUBLIC_APP_URL` | URL деплоя | После первого деплоя на Vercel |
   | `NEXT_PUBLIC_TELEGRAM_BOT_USERNAME` | Username бота | @BotFather → `/mybots` |
   | `TELEGRAM_BOT_TOKEN` | Токен бота | @BotFather → `/token` |

3. **Проверьте workflow:**
   - После push в `main`, проверьте вкладку **Actions** в GitHub
   - Workflow должен автоматически запуститься

## 📋 Чеклист настройки

- [ ] Репозиторий подключен к Vercel
- [ ] Все переменные окружения добавлены в Vercel
- [ ] Первый деплой успешно завершён
- [ ] `NEXT_PUBLIC_APP_URL` обновлён на реальный URL
- [ ] Telegram бот настроен с правильным URL
- [ ] Supabase база данных настроена
- [ ] SQL схема выполнена в Supabase

## 🐛 Troubleshooting

### Деплой не запускается автоматически

- Проверьте, что репозиторий правильно подключен к Vercel
- Убедитесь, что вы пушите в ветку `main`
- Проверьте логи в Vercel Dashboard

### Ошибки при сборке

- Проверьте, что все переменные окружения установлены
- Убедитесь, что Node.js версия совместима (18+)
- Проверьте логи сборки в Vercel Dashboard

### GitHub Actions не работает

- Проверьте, что все секреты добавлены правильно
- Убедитесь, что workflow файлы находятся в `.github/workflows/`
- Проверьте логи в GitHub Actions

## 📚 Дополнительные ресурсы

- [Документация Vercel](https://vercel.com/docs)
- [GitHub Actions](https://docs.github.com/en/actions)
- [Next.js Deployment](https://nextjs.org/docs/deployment)

